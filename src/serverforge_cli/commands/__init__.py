from serverforge_cli.commands import system, ssh, git, command_line_custom, timezone, ufw, fail2ban, hostname, users, cron, swap

MODULES = [system, ssh, git, command_line_custom, timezone, ufw, fail2ban, hostname, users, cron, swap]
